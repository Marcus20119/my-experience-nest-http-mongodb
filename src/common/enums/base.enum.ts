export enum Language {
  EN = 'en',
  VI = 'vi',
}

export enum SortType {
  ASC = 'asc',
  DESC = 'desc',
}

export enum SyncAction {
  CREATE = 'create',
  DELETE = 'delete',
  UPDATE = 'update',
}
